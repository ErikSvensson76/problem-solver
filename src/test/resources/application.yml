spring:
  datasource:
    url: "jdbc:h2:mem:test"
    username: "sa"
    password: ""
    driver-class-name: "org.h2.Driver"
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.H2Dialect"
  profiles:
    active: "test"